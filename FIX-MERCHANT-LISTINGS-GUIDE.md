# 🛠 راهنمای رفع ارورهای Merchant Listings در Google Search Console

**دامنه:** https://mql5expert.ir — **تاریخ:** 2026-08-18

## مشکل چیست؟

4 محصول سایت (EA&EM-PRO، SuperTrend EA، Forex Fury Pro، GoldApex_WF)
هر کدام **۱ ایراد بحرانی + ۲ ایراد غیربحرانی** در بخش
**Merchant listings** گوگل دارند.

## ریشه مشکل

structured data نوع `Product` در `index.html` (و `head.html`) **ناقص** بود:

| فیلد | وضعیت قبلی | وضعیت جدید (اصلاح‌شده) |
|------|------------|------------------------|
| `image` | ❌ نداشت ← **علت Critical** | ✅ اضافه شد |
| `sku` / `mpn` | ❌ نداشت | ✅ اضافه شد |
| `priceValidUntil` | ❌ نداشت | ✅ اضافه شد |
| `itemCondition` | ❌ نداشت | ✅ اضافه شد |
| `aggregateRating` | ❌ نداشت | ✅ اضافه شد |
| `seller` | ❌ نداشت | ✅ اضافه شد |

## ✅ کارهای لازم (به ترتیب)

### ۱) آپلود ۴ تصویر محصول
فایل‌های HTML به این تصاویر ارجاع می‌دهند؛ **باید حتماً** این ۴ فایل
روی سایت (ریشه GitHub Pages) آپلود شوند وگرنه گوگل باز هم critical می‌گیرد
(چون تصویر 404 می‌شود):

- `EA-EM-PRO.jpg`
- `SuperTrend-EA.jpg`
- `ForexFury-Pro.jpg`
- `GoldApex-WF.jpg`

> تصویر باید حداقل 1200px عرض داشته باشد و با فرمت JPG/PNG/WebP باشد.

### ۲) آپلود فایل‌های HTML اصلاح‌شده
فایل‌های زیر را با نسخه اصلاح‌شده جایگزین کنید:
- `index.html`
- `head.html`

### ۲ب) آپلود ۴ صفحه محصول مستقل (توصیه‌شده ✅)
برای رفع قطعی ارورها، ۴ صفحه محصول جداگانه ساخته شد که هرکدام Product schema کامل و مستقل دارند:

- `products/ea-em-pro.html`
- `products/supertrend-ea.html`
- `products/forex-fury-pro.html`
- `products/goldapex-wf.html`

این ۴ صفحه + آدرس‌هایشان در `sitemap.xml` ثبت شده و صفحه اصلی به آن‌ها لینک داده شده است.

### ۲ج) تغییرات صفحه اول (کارت‌های محصولات) ⭐
صفحه اول (`index.html`) حالا محصولات را به‌صورت **کارت‌هایی در یک ردیف (Grid)** با
عکس/ویدیو و دکمه «خرید محصول» نمایش می‌دهد:

- هر کارت شامل **پیش‌نمایش ویدیویی** محصول (/videos) است که با کلیک بزرگ‌نمایی می‌شود.
- دکمه **«خرید محصول»** روی هر کارت: محصول را در فرم پرداخت انتخاب کرده و به ناحیه خرید اسکرول می‌کند.
- لینک **«مشاهده جزئیات و دانلود»** به صفحه محصول همان آیتم می‌رود.
- منوی بالا: به‌جای «دانلودها»، حالا **«محصولات»** است و آیتم‌هایش به صفحات محصول لینک می‌شوند.

فایل‌های مربوطه:
- `products.css` → استایل کارت‌ها (جدید، باید آپلود شود)
- `index.html` → بازنویسی‌شده با گرید کارت‌ها

نکته: قیمت/نام/توضیح کارت‌ها از ورکر (`/products`) به‌صورت زنده بارگیری و جایگزین می‌شود.

### ۳) Purge Cache در Cloudflare
در داشبورد Cloudflare → Caching → Purge Everything.

### ۴) درخواست بررسی مجدد در Search Console
1. بروید به **URL Inspection** (نوار بالای GSC).
2. آدرس `https://mql5expert.ir/` را وارد کنید.
3. روی **Request Indexing** بزنید.
4. بعد از ۱-۲ روز به **Merchant listings** بروید و Validation را بررسی کنید.

## ⚠️ نکات مهم

1. اگر `aggregateRating` (امتیاز/نظر) واقعی ندارید، بهتر است آن را **حذف کنید**
   تا گوگل آن را محتوای ساختگی (spam) تشخیص ندهد. امتیاز ساختگی می‌تواند
   باعث **نادیده گرفتن کل rich result** شود. (در این فایل مقادیر نمونه گذاشته شده؛
   یا واقعی بگذارید یا حذفش کنید.)
2. اگر `price` واقعاً 10 دلار است مشکلی نیست؛ فقط مطمئن شوید `priceCurrency`
   همیشه با آن هماهنگ است.
3. برای بهترین نتیجه، پیشنهاد می‌شود به‌جای تعریف همه ۴ محصول در یک صفحه
   اصلی، **برای هر محصول یک صفحه جداگانه** (`/products/ea-em-pro.html` و...)
   بسازید و markup هر محصول را فقط در همان صفحه بگذارید. این روش استاندارد
   گوگل برای Merchant listings است و ارورها به‌طور قطعی برطرف می‌شود.

---

© All rights reserved K_M 2025 | info@tsgcoltd.ir
